// create a function that removes the last candy from the jar and returns the uploaded jar.

function eatCandy(candyJar) {
    // Remove the last candy from the jar and return the updated jar
    candyJar.pop()
    return candyJar
}
