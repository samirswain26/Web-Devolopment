// create a function that count the number of apples in the basket using a loop.

function countApples(apples) {
    let count = 0;
   for (let i = 0; i < apples; i++) {
     count++
   }
   return count;
 }
 