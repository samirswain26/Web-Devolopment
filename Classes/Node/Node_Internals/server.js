const http = require("http")
const url = require("url")

const routes = {
    GET: {},
    POST:{}
}

function handler(req,res){
    switch(req.url){
        case http.get : {
            
        }
    }
    
}